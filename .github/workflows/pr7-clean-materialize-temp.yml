name: PR7 clean materialize

on:
  push:
    branches:
      - feat/who-am-i-pr7-nfl-b-person-identity-clean
    paths:
      - .github/workflows/pr7-clean-materialize-temp.yml

permissions:
  contents: write

jobs:
  materialize:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "staging validated historical PR7 transport"
