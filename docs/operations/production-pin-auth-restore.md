# Production PIN Authentication Restore

Temporary operational branch used to redeploy the exact PIN authentication implementation currently stored on production `main`.

This branch must not be merged. After the original backend is redeployed and a disposable profile completes the full PIN login/session path, close this PR and retire PR #41.
