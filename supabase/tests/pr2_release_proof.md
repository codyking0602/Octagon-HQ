# PR 2 release proof

This temporary branch-only marker triggers the canonical Supabase verification workflow so it can prove the merged `main` deployment marker and live shell before the branch is closed unmerged.
