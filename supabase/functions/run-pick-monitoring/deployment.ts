// Replaced with the exact commit SHA only inside the trusted deployment workspace.
// Keep this function-owned marker in monitoring runtime changes so main deploys the shared src imports too.
export const DEPLOYED_SOURCE_SHA = "manual-monitoring-runner-v1";
