# Progressive Picks lock defaults

Published Picks cards use estimated 30-minute fight slots. The opening included bout locks at the published main-card start; every later bout locks 30 minutes after the prior slot, ending with the main event.

These are conservative default estimates, not claimed UFC walkout times. The existing event-wide lock remains the explicit master override, and the existing per-bout owner mutation remains the only adjustment owner.
