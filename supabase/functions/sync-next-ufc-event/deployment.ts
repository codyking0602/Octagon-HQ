// The backend deployment workflow replaces this file in its checked-out workspace
// before bundling the Edge Function so production can prove its exact source SHA.
export const DEPLOYED_SOURCE_SHA = "development";
